from file import File

rows = File.Db()
for row in rows:
    print(row['pass'])



Люди добрые подскажиет учу Flask, а именно пытаюсь сделать небольшое приложение
интересует следующее есть ссылка  <a href="user/?id={{ item['id'] }}">{{ item['login'] }}</a>
при нажатии на ссылку получаетьс такой URL http://127.0.0.1:5000/user/?id=2
по моей логике мы должны попась на страницу того товара(человека) чей id мы передаём в URL
Вопрос: как принять переданный в URL GET запрос и сформировать страницу


